using UnityEngine;

[CreateAssetMenu(
    fileName = "Power Array Variable",
    menuName = SOAHelper.ASSET_MENU_PATH + SOAHelper.PLAYER + "Power Token List Variable"
)]
public class PowerTokenListVariable : GenericListVariable<PowerToken>
{
}