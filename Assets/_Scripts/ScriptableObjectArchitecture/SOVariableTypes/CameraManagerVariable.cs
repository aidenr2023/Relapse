using UnityEngine;

[CreateAssetMenu(
    fileName = "Camera Manager Variable",
    menuName = SOAHelper.ASSET_MENU_PATH + SOAHelper.INSTANCE_HELPERS + "Camera Manager Variable"
)]
public class CameraManagerVariable : GenericVariable<CameraManager>
{
}